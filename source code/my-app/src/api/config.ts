// ─── Đổi địa chỉ này tuỳ theo môi trường chạy ────────────────────────────────
//   iOS Simulator    → http://localhost:3000
//   Android Emulator → http://10.0.2.2:3000
//   Thiết bị thật    → http://<IP_MÁY_TÍNH>:3000
//   (Mac: chạy `ipconfig getifaddr en0` để lấy IP)

export const API_BASE = "http://localhost:3000";
